# # # Autocorrelation Function Calculation for Each Index
# nlags = 10  # You can adjust this value if needed
# acf_values = {}

# for name in readable_names:
#     acf_val = acf(index_returns[name].dropna(), nlags=nlags)
#     acf_values[name] = acf_val
#     print(f"{name} ACF:", acf_val)
#     print('------------------------------------')

# # Getting the autocorrelations and partial autocorrelations for each series for signficance tests
# autocorrs = {}
# pacfs = {}

# for name in readable_names:
#     autocorr_vals = sm.tsa.acf(index_returns[name].dropna(), nlags=30, fft=True)
#     pacf_vals = sm.tsa.pacf(index_returns[name].dropna(), nlags=30)
#     autocorrs[name] = autocorr_vals
#     pacfs[name] = pacf_vals
    
# # Test autocorrelations
# bartlett_results_acf = bartlett(*autocorrs.values())
# print(f"Bartlett test result for autocorrelations: statistic={bartlett_results_acf[0]}, p-value={bartlett_results_acf[1]}")

# # Test partial autocorrelations
# bartlett_results_pacf = bartlett(*pacfs.values())
# print(f"Bartlett test result for partial autocorrelations: statistic={bartlett_results_pacf[0]}, p-value={bartlett_results_pacf[1]}")
